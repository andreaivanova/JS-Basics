function seats(input){
    let lastSector = input[0];
    //the first sector is the letter A
    let rowsFirstSector = Number(input[1]);
    let seatsOddRow = Number(input[2]);
      for (let i = 'A'; i <=lastSector ; i++) {
           for (let j = 1; j <= rowsFirstSector; j++) {
                for (let k = a; k < ; k++) {}
           }
      }

}
seats(['B','3','2'])