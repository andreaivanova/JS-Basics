function symbolSequence(input){
    let receivedInput = input[0];
    for(let i = 0; i <= receivedInput.length - 1; i++){
      let currentSymbol = receivedInput[i];
      console.log(currentSymbol)
  
  
    }
    
  
  
  }symbolSequence(['hello'])